import React from "react";
import { Text } from "react-native";


export default (props) => {
    return(
        <Text>
            O número entre {props.min} e {props.max} é 
        </Text>
    )
}